import { Router } from "express"

const router = Router()

router.post("/", async (req, res) => {

  return res.json({
    success: true,
    text: "Voice received",
  })
})

export default router